import styled from 'styled-components';

const StyledShoppingCartButton = styled.div`
  .cartIcon {
    height: 30px;
    width: 30px;

    &:hover {
      cursor: pointer;
    }
  }
`;

export default StyledShoppingCartButton;
